meta {
  name: Password Reset Check
  type: http
  seq: 8
}

head {
  url: {{baseUrl}}/1/account/recover/:passwordResetId/:secret
  body: none
  auth: inherit
}

params:path {
  passwordResetId:
  secret:
}

docs {
  Check if a password reset link is still valid. Returns 200 if valid, error otherwise.
}

meta {
  name: Delete Outgoing Invite
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/2/shares/user/invites/outgoing/:inviteId
  body: none
  auth: inherit
}

params:path {
  inviteId:
}

docs {
  Cancel a pending outgoing share invite.
}
